---
# Used only for DSM distribution with 7.1 distribution major version

docker_install_bridge_utils: false

docker_service_name: pkg-Docker-dockerd.service

docker_architecture_mapping:
  x86_64: x64

docker_codename_mapping:
  NA: "NA"

docker_dsm_synopkg_check_package: Docker
